/// Inspection symbology for 2020 collector map. 6 days passed changes symbology
var time1 = Date($feature["Date"])
if(DateDiff(Now(), time1, 'days')<6){
Var Status = "New";
}
else if(DateDiff(Today(), time1, 'days')>=6){
var Status = "Old";
}
else{
var Status = "Current";
}
return Status
